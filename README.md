#CRUD Application with express and pug

This is a CRUD application built with javascript, express framework, pug,  bootstrap. 
It also uses mapbox api for using location features within the application.

